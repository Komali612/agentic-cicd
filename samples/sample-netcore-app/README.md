# sample-netcore-app

A minimal **.NET Core 8** console app with an xUnit test and **no CI pipeline** —
a target repository for the NetCoreCI agent to discover and author a workflow
against. There is deliberately no `.github/workflows/` directory.
