# sample-netlegacy-app

A minimal **.NET Framework 4.8** (old-style `.csproj`) class library with **no CI
pipeline** — a target repository for the NetLegacyCI agent. There is deliberately
no `.github/workflows/` directory.
