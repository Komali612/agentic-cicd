name: CI
on: [push, workflow_dispatch]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      # agent-contracts + ci-authoring must be resolvable (index/git), like agent-core.
      - run: pip install ./ pytest
      - run: pytest -q
