# Per-agent Helm values. The chart itself is frozen.
image:
  repository: netlegacy-ci-agent
  tag: latest
  pullPolicy: IfNotPresent

replicas: 1

service:
  port: 8080

env:
  AGENT_NAME: netlegacy-ci
  AGENT_LOG_LEVEL: INFO

# GITHUB_TOKEN is a secret. The `netlegacy-ci-secrets` Secret is created + synced
# by the Doppler Kubernetes Operator (keys = Doppler secret names). See ../../SECRETS.md.
secretEnv:
  GITHUB_TOKEN: {name: netlegacy-ci-secrets, key: GITHUB_TOKEN}

resources: {}
